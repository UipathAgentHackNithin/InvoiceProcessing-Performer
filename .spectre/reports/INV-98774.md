# SpectreAI Report — INV-98774

**Process:** ICSAUTO-3201

**Diagnosis:** The logs explicitly show a clear system error during the processing of invoice INV-98774 by the InvoiceProcessing_Performer bot. The error message is 'Get TaxAmount (crashes â€” key missing): The given key was not present in the dictionary.' This error occurs in CalculateInvoiceTotals.xaml at Assign 'Get TaxAmount (crashes â€” key missing)' â€” the SpecificContent dictionary of the queue item does not contain the key TaxAmount, causing a KeyNotFoundException when accessed directly.

**Recommended action:** Investigate why the dictionary key for TaxAmount is missing during the CalculateInvoiceTotals workflow. Verify the input data for invoice INV-98774 to ensure all required fields, especially TaxAmount, are present and correctly formatted. Update the bot to handle the missing key gracefully by adding a null/key guard before accessing TaxAmount from SpecificContent.

**Analysis:** The failure is caused by accessing the dictionary key 'TaxAmount' directly without checking if it exists, causing a KeyNotFoundException. The fix adds a guard using If and ContainsKey to check for the key's presence before accessing it. If missing, it defaults to "0" to allow the calculation to proceed without crashing. This minimal change prevents the exception and handles missing TaxAmount gracefully.
