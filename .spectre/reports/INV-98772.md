# SpectreAI Report — INV-98772

**Process:** ICSAUTO-3201

**Diagnosis:** NullReferenceException in CalculateInvoiceTotals.xaml â€” SpecificContent key TaxAmount missing from queue item. Split() also uses String literal instead of Char() array.

**Recommended action:** Add null guard on TaxAmount lookup. Change Split(".") to Split("."c) in CalculateInvoiceTotals.xaml.

**Analysis:** The primary cause of the NullReferenceException is the missing 'TaxAmount' key in the SpecificContent dictionary of the queue item. The fix adds a null guard by checking if the key exists before accessing it, defaulting to "0" if missing to prevent the exception. The secondary fix corrects the Split method argument from a string literal to a character literal, which is the correct VB syntax. Both fixes are minimal and localized to the Assign activities in CalculateInvoiceTotals.xaml.
