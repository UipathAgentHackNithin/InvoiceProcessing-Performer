# SpectreAI Report — INV-98772

**Process:** ICSAUTO-3201

**Diagnosis:** NullReferenceException in CalculateInvoiceTotals.xaml â€” SpecificContent key TaxAmount missing from queue item. Split() also uses String literal instead of Char() array.

**Recommended action:** Add null guard on TaxAmount lookup. Change Split(".") to Split("."c) in CalculateInvoiceTotals.xaml.

**Analysis:** The NullReferenceException occurs because the key 'TaxAmount' is missing in the SpecificContent dictionary, and the code calls .ToString on a Nothing value. Adding a null guard using ContainsKey prevents this exception by providing a default value '0' when the key is missing. Additionally, the Split method uses a String literal "." which is invalid in VB; it must use a Char literal "."c. This fix changes the Split calls accordingly. Both fixes are localized and can be applied as snippet replacements in the existing XAML.
